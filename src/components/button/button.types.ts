export type ButtonProps = {
  text: string;
  type: 'operator' | 'number' | 'equal'
}

export type AditioanalStyledProps = {
  leftTop?: boolean;
  rightTop?: boolean;
  width: number
  height: number;
}